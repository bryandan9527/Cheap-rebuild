# Artifact Checks

## Corpus
- 只允許 `編號 + 內文`
- 不允許 markdown 標題
- 不允許刪除線標記

## Records
- 高信心樣本都要有：
  - `reasoning_pattern`
  - `primary_route`
  - `topic_tags`
  - `angle_statement`
  - `cheap_push_moves`

## Taxonomy
- reasoning 要能覆蓋多個 topic cluster
- route 的 `must_include_moves` 必須真的包含 cheap 推進方式
